NEXT_PUBLIC_API_URL=http://localhost:4000/api/v1
# In Vercel's project settings (not this file), set the production value to:
# NEXT_PUBLIC_API_URL=https://reel-backend-a2sz.onrender.com/api/v1
# lib/api.js already prefixes every request path with this value, so once
# it's set correctly, calls land on /api/v1/auth/login etc. automatically —
# no other frontend code changes are needed.
